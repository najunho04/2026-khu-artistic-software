# history.md — 실수 기록 (재발 방지 대장)

> 이 문서의 존재 이유는 하나입니다. **같은 실수를 두 번 하지 않기 위해서.**
> 여기에 적힌 항목이 다시 발생하면 그것은 규칙 위반입니다.

## 사용법

1. **작업 시작 전** 이 문서를 처음부터 끝까지 읽는다.
2. 실수를 했거나 지적을 받으면 **그 자리에서** 아래 형식으로 항목을 추가한다.
3. 항목은 지우지 않는다. 해결되어도 남겨두고 `상태`만 바꾼다.
4. 새 항목 번호는 `H-0NN` 형식으로 이어서 매긴다.

### 항목 작성 형식

```
### H-0NN. (한 줄 제목)
- 발생일:
- 상태: 재발방지 적용 / 관찰 중
- 무슨 일이: (사실만)
- 왜 생겼나: (근본 원인 1가지)
- 다시 안 하려면: (다음 번에 실제로 할 행동. 추상적 다짐 금지)
- 관련 문서: ERD.md / API.md / ROADMAP.md 중 해당 절
```

## 매 작업 전 체크리스트

- [ ] 이번 작업이 `ROADMAP.md`상 **현재 Phase 범위 안**인가
- [ ] 요청에 문서에 없는 항목이 섞여 있지 않은가 → 있으면 **질문 후 대기**
- [ ] 실패하는 테스트를 먼저 작성했는가 (TDD)
- [ ] ERD / API / ROADMAP **세 문서를 함께** 갱신했는가 (영향 없으면 "영향 없음" 명시)
- [ ] 세 문서의 `최종 수정` 날짜가 같은가
- [ ] 변수·클래스명을 줄이지 않았는가 (줄였다면 주석에 풀네임)
- [ ] 주석에 markdown을 쓰지 않고 강조를 큰따옴표로 했는가

---

## 기록

### H-001. 로드맵이 실제 코드 상태와 어긋난 채로 "완료"라고 적혀 있었다
- 발생일: 2026-09-04
- 상태: 재발방지 적용
- 무슨 일이: `ROADMAP.md` 1-1 이 "프로젝트 초기 세팅 - 완료"이고 항목에 "CI 파이프라인"이 포함돼 있었으나, 실제로는 `.github/workflows` 디렉터리 자체가 없었다. 같은 절의 "패키지 구조 device / deviceapi 분리"도 소스에 존재하지 않았다.
- 왜 생겼나: 로드맵을 Notion 문서에서 역산해 만들면서 "하기로 되어 있는 것"과 "실제로 되어 있는 것"을 구분하지 않고 한 줄에 섞어 적었다.
- 다시 안 하려면: 로드맵에 "완료"라고 쓰기 전에 반드시 해당 산출물의 존재를 명령으로 확인한다. 파일이면 `ls`, 테스트면 `./gradlew test --rerun-tasks` 로 실제 실행 결과를 본다. 확인한 근거(명령과 결과)를 로드맵 본문에 함께 남긴다. 부분만 된 항목은 "완료"가 아니라 항목별로 완료/미착수를 쪼개 적는다.
- 관련 문서: ROADMAP.md 1-1, 0장 현재 상태 표

### H-002. gradle test 가 UP-TO-DATE 로 건너뛴 것을 "통과"로 읽을 뻔했다
- 발생일: 2026-09-04
- 상태: 재발방지 적용
- 무슨 일이: 테스트 환경을 검증하려고 `./gradlew test` 를 돌렸는데 `Task :test UP-TO-DATE` 가 나오며 1초 만에 BUILD SUCCESSFUL 이 떴다. 실제로는 테스트가 한 개도 실행되지 않았다.
- 왜 생겼나: Gradle 의 증분 빌드(입력이 안 바뀌면 태스크를 건너뛰는 기능)를 고려하지 않고 초록불만 보았다.
- 다시 안 하려면: 테스트 결과를 근거로 무언가를 주장할 때는 항상 `./gradlew test --rerun-tasks` 를 쓴다. 그리고 `build/test-results/test/*.xml` 의 `tests=` 와 `skipped=` 숫자를 눈으로 확인한다. skip 이 0 이 아니면 통과가 아니다.
- 관련 문서: ROADMAP.md 0-A 테스트 환경

### H-003. 테스트가 문서에 적힌 것과 다른 DB 메이저 버전에서 돌고 있었다
- 발생일: 2026-09-04
- 상태: 재발방지 적용
- 무슨 일이: `TestcontainersConfiguration` 과 `compose.yaml` 이 이미지 태그를 `postgres:latest` 로 두고 있었다. 확인해보니 latest 가 이미 PostgreSQL 18 이었고, `ERD.md`·`ROADMAP.md` 가 확정한 17 이 아니었다. 아무도 코드를 건드리지 않았는데 문서와 실제가 어긋나 있었다.
- 왜 생겼나: Spring Initializr 가 만들어준 기본 설정을 그대로 두었다. 기본값이 프로젝트의 확정 사항과 일치하는지 확인하지 않았다.
- 다시 안 하려면: 외부 이미지·라이브러리 버전은 메이저 버전까지 태그에 고정한다. `latest` 를 쓰지 않는다. 그리고 고정이 풀리는 것을 테스트로 잡는다. 이번에는 실제로 뜬 DB 에 접속해 메이저 버전을 확인하는 `PostgreSqlVersionIntegrationTest` 를 추가했다. 태그 문자열이 아니라 실제 동작을 확인해야 태그와 이미지가 어긋나는 경우까지 잡힌다.
- 관련 문서: ROADMAP.md 0-A 테스트 환경, ERD.md 헤더 DBMS 항목

### H-004. ERD 에 없는 컬럼을 임의로 추가했다
- 발생일: 2026-09-04
- 상태: 재발방지 적용
- 무슨 일이: 1-3 스키마 마이그레이션에서 `children`, `routine_templates`, `big_routines`, `small_routines`, `comments` 5개 테이블에 `created_at` 컬럼을 넣었다. `ERD.md` 의 mermaid 정의에는 이 5개 테이블에 `created_at` 이 없다. 사용자에게 묻지 않고 넣었다.
- 왜 생겼나: "생성 시각은 당연히 있어야 한다" 는 일반적인 관행을 문서보다 앞세웠다. CLAUDE.md 0-1 이 금지한 "임의 구현 후 통보" 를 그대로 저질렀다.
- 다시 안 하려면: 스키마나 API 를 만들 때, 작성이 끝난 뒤 문서와 산출물을 사람 눈이 아니라 스크립트로 대조한다. 이번에는 ERD 의 mermaid 블록에서 테이블별 컬럼을 뽑아 마이그레이션 SQL 의 컬럼과 집합 비교해서 잡았다. 이 대조를 커밋 전에 반드시 한 번 돌린다. "당연히 필요한 것" 이라는 판단이 들면 그것이 바로 질문해야 하는 신호다.
- 관련 문서: ERD.md 1장·2장 mermaid 정의, ROADMAP.md 1-3

### H-005. 내가 만든 용어("프리셋")를 풀어 쓰지 않고 계속 썼다
- 발생일: 2026-09-09
- 상태: 재발방지 적용
- 무슨 일이: 템플릿의 성격을 설명하면서 "프리셋" 이라는 단어를 썼다. 처음 한 번은 "프리셋(저장해둔 양식)" 이라고 괄호를 달았지만, 그 뒤로는 계속 "프리셋" 단독으로 썼다. 사용자가 "프리셋 수정이 무슨 소리지" 라고 되물어 대화가 한 번 멈췄다.
- 왜 생겼나: 한 번 괄호로 풀었으니 그 뒤로는 통했다고 혼자 판단했다. CLAUDE.md 4장은 "바로 쉬운 말로 한 번 더" 풀라고 했을 뿐, 한 번 풀면 그 뒤로 마음대로 써도 된다고 하지 않았다.
- 다시 안 하려면: 문서와 사용자가 이미 쓰고 있는 단어를 그대로 쓴다. 여기서는 "템플릿" 이 세 문서에 모두 등장하는 정식 명칭이므로 그것만 쓴다. 새 단어를 꺼내야 할 때는 그 단어가 문서에 있는지 먼저 확인하고, 없으면 꺼내지 않는다. 설명이 필요하면 새 이름을 붙이는 대신 문장으로 풀어 쓴다.
- 관련 문서: CLAUDE.md 4장 커뮤니케이션 규칙, API.md 10장, ERD.md ROUTINE_TEMPLATES

### H-006. 문서에 이미 있는 에러 코드를 모르고 같은 뜻의 코드를 하나 더 만들었다
- 발생일: 2026-09-09
- 상태: 재발방지 적용
- 무슨 일이: 로그인 방식을 이메일·비밀번호로 바꾸면서 "API.md" 3-2 에 `AUTH_UNAUTHORIZED`(401, "로그인이 필요합니다.") 를 새로 넣었다. 그런데 3-1 공통 표에 `UNAUTHORIZED`(401, "로그인이 필요합니다.") 가 이미 있었다. 상태 코드도 문구도 완전히 같은 코드가 두 개가 됐다. 코드를 쓰기 전에 발견해 문서에서 지웠다.
- 왜 생겼나: 3-2 표만 보고 고쳤다. 에러 코드는 3-1 부터 3-6 까지 여섯 개 표에 나뉘어 있는데, 새 코드를 추가할 때 "전체 목록" 을 한 번도 펼쳐보지 않았다.
- 다시 안 하려면: 에러 코드를 추가할 때는 3장 전체에서 코드 이름을 뽑아 한 줄씩 나열해 보고 나서 넣는다. 이름뿐 아니라 "상태 코드와 문구가 같은 것" 도 함께 본다. 이름이 다르면 중복이 아니라고 착각하기 쉽기 때문이다. "API.md" 2-2 가 "앱은 code 로 분기한다" 고 정했으므로, 같은 뜻의 코드가 둘이면 앱이 어느 쪽으로 분기해야 할지 알 수 없게 된다.
- 관련 문서: API.md 3-1 · 3-2, common/ErrorCode.java, ErrorCodeTest

### H-007. 테스트가 확인하는 범위가 좁아 "절반만 구현된 기능" 이 초록불로 통과했다
- 발생일: 2026-09-09
- 상태: 재발방지 적용
- 무슨 일이: 빅루틴 생성에서 `templateId` 를 주면 양식의 값이 복사되어야 하는데, 실제로는 **제목만** 복사되고 시각과 할 일 목록은 무시되고 있었다. 할 일이 딸려오지 않으므로 양식을 저장해 둔 이유가 거의 사라지는 상태였다. 그런데 "양식을 수정해도 이미 만든 빅루틴은 그대로다" 테스트가 초록이었다. 그 테스트가 제목 하나만 확인했기 때문이다. 사용자가 "템플릿은 그 정보로 루틴을 만들어 주는 거지" 라고 물어 코드를 다시 읽다가 발견했다.
- 왜 생겼나: 테스트를 "이 기능이 도는가" 가 아니라 "내가 방금 구현한 줄이 도는가" 기준으로 썼다. 제목 복사를 구현하면서 제목만 확인하는 단언을 달았고, 시각과 할 일은 애초에 구현하지 않았으니 확인할 생각도 하지 않았다. 구현과 테스트를 같은 머릿속 목록으로 만들면 빠뜨린 항목은 양쪽에서 똑같이 빠진다.
- 다시 안 하려면: 무언가를 "복사한다" 또는 "함께 처리한다" 고 적을 때는, 대상 필드를 **문서에서 세어** 그 개수만큼 단언을 단다. 여기서는 `ROUTINE_TEMPLATES` 의 컬럼이 `title`·`start_time`·`end_time`·`small_routines` 네 개였으므로 단언도 네 개여야 했다. 그리고 문서가 "값이 복사된다" 처럼 대상을 적지 않고 있으면 그것부터 고친다. 무엇이 복사되는지 문서에 없으면 구현이 절반만 해도 어긋났다고 잡히지 않는다.
- 관련 문서: API.md 9장 templateId, ERD.md ROUTINE_TEMPLATES, RoutineService.resolveTitle
