# CLAUDE.md — 예소(Yeso) 백엔드 작업 규칙

## 0. 최우선 원칙
1. **문서에 없는 것은 구현하지 않는다.** 빠진 파트·파이프라인·필드를 발견하면 멈추고 사용자에게 **먼저 질문**한다. 임의 구현 후 통보는 금지.
2. 작업 시작 전 `history.md`를 읽는다. 거기 적힌 실수는 **두 번 반복하지 않는다.**
3. 실수했거나 지적받으면 그 자리에서 `history.md`에 항목을 추가한다.

## 1. 진실의 출처 (권한 이양)
| 영역 | 이 문서를 따른다 |
|---|---|
| 테이블·컬럼·제약조건·인덱스 | `ERD.md` |
| 엔드포인트·req/res·에러코드·공통 응답 포맷 | `API.md` |
| 구현 순서·Phase·범위·블로커 | `ROADMAP.md` |

- 이 3문서가 내 기억·추측보다 **항상 우선**한다. 문서끼리 충돌하면 구현을 멈추고 질문한다.
- "❌ 제외"로 적힌 범위는 **구현하지 않는다.**
- "미확정 / 회의 대기" 사항은 **임의로 정하지 않고 질문**한다.

## 2. 문서 동기화 (필수)
ERD / API / ROADMAP 은 **항상 함께 최신화**한다.
- 한 문서가 바뀌면 나머지 두 문서에 파급이 있는지 점검하고, 없으면 "영향 없음"이라고 명시한다.
- 세 문서 상단의 `최종 수정` 날짜를 갱신한다.

## 3. TDD (테스트 주도 개발)
Red → Green → Refactor 순서를 지킨다.
1. **실패하는 테스트 먼저** 작성하고, 실패하는 것을 눈으로 확인한다.
2. 그 테스트를 통과할 만큼만 구현한다.
3. 테스트가 초록인 상태를 유지하며 리팩터링한다.

- 테스트 케이스는 `API.md`의 성공·에러 응답과 `ERD.md`의 제약조건에서 도출한다.
- 프로덕션 코드를 테스트보다 먼저 쓰지 않는다.

## 4. 커뮤니케이션 규칙
- 어려운 전문용어를 쓰면 **바로 쉬운 말로 한 번 더** 풀어 설명한다.
  예: 멱등성(같은 요청을 여러 번 보내도 결과가 한 번 보낸 것과 같은 성질)
- 코드 식별자를 언급할 때는 **괄호로 짧은 키워드 설명**을 붙인다.
  예: `insertUserDataInterface` (DB 유저 데이터 삽입 인터페이스) 를 수정해야 합니다.

## 5. 코드 작성 규칙
- 변수명·클래스명을 **줄이지 않는다.** 너무 길어 부득이 줄였다면 주석에 풀네임을 남긴다.
  예: `int rtnCnt; // routineCount (루틴 개수)`
- 주석은 상세히 남긴다. 무엇을 하는지뿐 아니라 **왜 그렇게 했는지**까지 적는다.
- 주석 안에서 markdown 등 서식 표현을 쓰지 않는다. 강조는 **큰따옴표만** 사용한다.
  예: // 이 UPDATE 는 "멱등성" 보장을 위해 INSERT 대신 사용한다
- DB 컬럼은 snake_case, API 필드는 camelCase (`API.md` 1-2).

## 6. 기술 스택 · 구조 (ROADMAP 1-1, 1-2)
- Spring Boot 4.1.1 / Java 21 / Gradle 9.7.1 / PostgreSQL 17
- `device`(앱용) 패키지와 `deviceapi`(기기용) 패키지를 **분리**한다. 인증 방식이 다르기 때문.
- SecurityFilterChain 2개 분리: 앱 `/api/v1/**`(보호자 JWT) / 기기 `/device-api/v1/**`(opaque 토큰).
- 시간은 서버 UTC 저장(`timestamptz`), `time` 컬럼은 KST 벽시계 시각으로 해석한다.
